# AC-12 — `seed_oracle.py`: STAFF schema + 10 synthetic records

| Field | Value |
|---|---|
| Jira story | [AC-12](https://cloudandthings.atlassian.net/browse/AC-12) — E2-S2 seed_oracle.py STAFF schema + seed |
| Epic | [AC-3](https://cloudandthings.atlassian.net/browse/AC-3) — Procurement Data Access |
| Zone | 🟢 Green (`backend/scripts/seed_oracle.py`) |
| Ratified on | 2026-05-18 |
| Ratified by | C&T Project Lead |
| Status | Ratified — implementation may proceed |

## Synthetic-PII alert

This script writes `FULL_NAME` and `EMAIL` rows into the STAFF table.
**All values are synthetic per [ADR-007](../adr/ADR-007-synthetic-data-and-idme-sandbox.md)
+ NFR-08** — no real FCPS staff data ever appears in this script or the
seed it produces. The email domain is `test.com` to leave no doubt
about the synthetic-ness (the names are obviously test names too;
between them there is no plausible re-identification risk).

## DATA_MODEL §8 vs Jira description

The Jira description listed "1 ADMIN, 2 L2, 5 L1, 1 L0 = 9 records".
[DATA_MODEL.md §8](../DATA_MODEL.md#L190) has **10 records** with **2
ADMINs** (Sarah Mitchell, James Okafor). DATA_MODEL is authoritative per
[ADR-010](../adr/ADR-010-no-migration-tool.md). Jira description fixed
in the same commit.

## Decisions

| # | Decision | Ratified value |
|---|---|---|
| AC12-D1 | Location | `backend/scripts/seed_oracle.py`, runnable as `python -m scripts.seed_oracle` or `python scripts/seed_oracle.py` from `backend/`. |
| AC12-D2 | Idempotency | `DROP TABLE STAFF` (catching ORA-00942 "table does not exist") → `CREATE TABLE STAFF` → batch INSERT of 10 rows. DROP+CREATE not TRUNCATE so the schema is regenerated on every run — ADR-010 says the script is the schema authority. |
| AC12-D3 | Schema | Exact match to DATA_MODEL.md §4.1: `STAFF_ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY`, every other column `NOT NULL`. `EMPLOYEE_ID` UNIQUE, `EMAIL` UNIQUE. `IDME_VERIFIED CHAR(1) CHECK IN ('Y','N')` + same for `ACTIVE`. `ROLE CHAR(10) CHECK IN ('ADMIN','STAFF')`. `PROCUREMENT_LEVEL NUMBER(1) CHECK BETWEEN 0 AND 3`. `CREATED_DATE / UPDATED_DATE TIMESTAMP WITH TIME ZONE DEFAULT SYSTIMESTAMP`. |
| AC12-D4 | SQL style | Module-level constants (`_DROP_TABLE_STAFF`, `_CREATE_TABLE_STAFF`, `_INSERT_STAFF`). INSERT uses named bind variables — same parameter-bind discipline as AC-11. Never f-string into SQL. |
| AC12-D5 | Connection strategy | Direct `oracledb.connect()` — this is a CLI, not a request handler. Reads `ORACLE_*` from the environment via `app.core.config.get_settings()` so a runner doesn't need a parallel env-loading mechanism. |
| AC12-D6 | Seed data | 10 records from DATA_MODEL.md §8 verbatim (EMPLOYEE_ID, FULL_NAME, DEPARTMENT, JOB_TITLE, LEVEL, ROLE). Emails synthesised as `firstname.lastname@test.com` (lowercase, dot-separated). All `IDME_VERIFIED='Y'` and `ACTIVE='Y'` — verification-state edge cases are covered by the dev_auth `level_zero` / `not_registered` personas, not by storing inactive rows. |
| AC12-D7 | Output | `print()` with checkmarks for human-readable progress (`✓ Dropped STAFF`, `✓ Created STAFF`, `✓ Inserted 10 STAFF rows`). Exit code 0 on success, 1 on any failure. No logger — this is a script, the stdout IS the audit trail. |
| AC12-D8 | Tests | `oracledb.connect` mocked at the boundary, same pattern as AC-11. Assertions: DROP runs first, CREATE contains all required columns + constraints, exactly 10 INSERTs run with the correct bind values, the data matches DATA_MODEL.md §8, idempotency (re-running calls DROP again). |
| AC12-D9 | dev_auth realignment | Update `_GRANTED_PERSONAS` in `backend/app/api/dev_auth.py` so `staff_l2 → staff_id=6` (matches FCPS-006 / David Hernandez, the lowest STAFF-L2 row in the seed). `admin_l3 → staff_id=1` and `staff_l1 → staff_id=3` were already correct. ADR-014 and DEV-AUTH-persona-picker.md DEV3 amended with a 2026-05-18 correction note. Reason: once AC-13 wires the real `/callback`, any introspection of a dev-session's `staff_id` against Oracle should resolve to the role/level it claims to be — otherwise demos are confusing. |
| AC12-D10 | PROCUREMENT_ITEMS | **Out of scope for AC-12.** Story is STAFF only. Vendor seed ships in a later story. |

## Files this story creates / modifies

| Path | Zone | Change |
|---|---|---|
| `backend/scripts/__init__.py` | — | New (empty package marker) |
| `backend/scripts/seed_oracle.py` | 🟢 Green | **New** — runnable seed script |
| `backend/tests/test_seed_oracle.py` | — | New (~5 tests) |
| `backend/app/api/dev_auth.py` | 🔴 Red | One-line `_GRANTED_PERSONAS` realignment (D9) |
| `backend/tests/test_dev_auth.py` | — | Update `staff_l2` expected staff_id (`2 → 6`) |
| `docs/adr/ADR-014-demo-persona-login-dev-only.md` | — | Correction note + table update for `staff_l2` row |
| `docs/decision-log/DEV-AUTH-persona-picker.md` | — | DEV3 table update + correction note |

## Open follow-ups

- **PROCUREMENT_ITEMS seed** — separate story (likely AC-15 or similar
  per the backlog).
- **Integration smoke against real Oracle XE** — `pytest.mark.oracle_integration`
  marker reserved in AC-11 still unused; will be wired up when Oracle XE
  joins docker-compose.
